name: 02 - workflow events

on:
    push:

jobs:
    echo:
        runs-on: ubuntu-latest
        steps:
            - name: My first event
              run: echo "I have been triggered as ${{github.event_name}} event"
            