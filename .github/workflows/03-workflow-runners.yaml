name: 03 - workflow runners 

on:
    push:

jobs:
    ubuntu-echo:
        runs-on: ubuntu-latest
        steps:
          - name: show os verison
            run: |
                echo "This job is running on Ubuntu runner"
                echo "Runner OS: $RUNNER_OS"